export interface Bookmark {
    _id: string;
    articleId: string;
    _ownerId: string;
    createdAt: string;
    updatedAt: string;
}

export interface Like {
    _id: string;
    articleId: string;
    _ownerId: string;
    createdAt: string;
    updatedAt: string;
}

export interface ReadArticle {
    _id: string;
    articleId: string;
    _ownerId: string;
    createdAt: string;
    updatedAt: string;
}
